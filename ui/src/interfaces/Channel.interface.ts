
export interface Channel {
    channel: string;
    channel_id: string;
    channel_url: string;
    uploader: string;
    uploader_id: string;
    uploader_url: string;
    thumbnail: string;
    description: string;
    channel_follower_count: number;
}